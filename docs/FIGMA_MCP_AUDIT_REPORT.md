# Figma MCP Integration Report

**Generated:** 2026-09-24 17:30 WIB (UTC+7)  
**Workspace:** `G:/Course LSM/UI-UX`  
**Generated by:** Hermes Agent (Oh My Pi harness) — manual verification run  
**Report file:** `G:/Course LSM/UI-UX/FIGMA_MCP_AUDIT_REPORT.md`

---

## 1. OMP Environment

- **OMP version:** Hermes Agent v0.21.1 (2026.9.7) · upstream `7de8728c` · local `1603a073 (+1 carried commit)` · Python 3.11.16 · OpenAI SDK 2.24.0 · Install method: git · Install directory: `C:\Users\hikari\AppData\Local\hermes\hermes-agent` · Update available: 7970 commits behind (non-blocking, tidak di-update pada tahap ini)
- **MCP config source:** `HERMES_HOME` platform default — resolved via `hermes_constants.get_hermes_home()` → `LOCALAPPDATA` fallback. Verified via `hermes config path`.
- **MCP config file:** `C:\Users\hikari\AppData\Local\hermes\config.yaml`
- **Secrets file:** `C:\Users\hikari\AppData\Local\hermes\.env`
- **Token storage:** `C:\Users\hikari\AppData\Local\hermes\mcp-tokens/` (per-server JSON, 0o600 equivalent ACL on Windows)
- **Schema reference:** `G:/hermes-agent/cli-config.yaml.example:1416-1452`, `website/docs/user-guide/features/mcp.md` (936 lines), `optional-mcps/figma/manifest.yaml`
- **Config version:** `_config_version: 42` · `hermes config check` → `Config version: 42 ✓`

---

## 2. Figma MCP Configuration

- **Server name:** `figma` (lowercase, sesuai `optional-mcps/figma/manifest.yaml:name: figma`; tidak ada duplikat — pre-install check `hermes mcp list` = `No MCP servers configured`)
- **Endpoint:** `https://mcp.figma.com/mcp` (tetap sesuai instruksi; tidak diubah ke localhost)
- **Transport:** `http` (Streamable HTTP / HTTP). Manifest mendeklarasikan `transport.type: http` + `url: https://mcp.figma.com/mcp`. Hermes memetakan ke `mcp_servers.figma.url` + `auth: oauth`. CLI menampilkan `Transport: HTTP → https://mcp.figma.com/mcp`. Bukan `stdio` dan bukan `http://127.0.0.1:3845/mcp` (Desktop MCP hanya alternatif).
- **Auth method:** `auth: oauth` (OAuth 2.1 PKCE + Dynamic Client Registration). Figma mewajibkan `client_name` exact — Hermes otomatis set `oauth.client_name: "Claude Code"` untuk host `mcp.figma.com` agar DCR tidak 403 (bare `Hermes Agent` akan 403; lihat `website/docs/user-guide/features/mcp.md:252-263` dan `tools/mcp_oauth.apply_oauth_provider_defaults`).
- **Raw config (YAML valid):**
  ```yaml
  mcp_servers:
    figma:
      url: https://mcp.figma.com/mcp
      auth: oauth
      enabled: true
  ```
- **Configuration valid:** **YES** — `yaml.safe_load` pass, `hermes config check` pass, `hermes mcp list` menampilkan `figma | https://mcp.figma.com/mcp | all | ✓ enabled`. Tidak ada konflik dengan server lain (0 → 1). Tidak ada plaintext credential di `config.yaml`.
- **Install method:** `hermes mcp install figma` (catalog Nous-approved, source: `https://developers.figma.com/docs/figma-mcp-server/remote-server-installation/`) — instalasi melakukan probe + OAuth browser flow + tool-selection checklist (40 tools, all enabled, no filter).

---

## 3. Connection

- **Server discovered:** **YES** — `hermes mcp list` dan `hermes mcp catalog` menampilkan `figma | enabled` setelah install.
- **Connected:** **YES** — `hermes mcp test figma` berhasil dua kali berurutan:
  ```
  Testing 'figma'...
  Transport: HTTP → https://mcp.figma.com/mcp
  Auth: OAuth 2.0
  ✓ Connected (1563ms / 2188ms)
  ✓ Tools discovered: 40
  ```
- **Authentication required:** **YES** — OAuth 2.1. URL authorize yang dibuka otomatis:
  `https://www.figma.com/oauth/mcp?response_type=code&client_id=g7FRsEwT3A5yG0dK5Tu0Gn&redirect_uri=http%3A%2F%2F127.0.0.1%3A51796%2Fcallback&scope=mcp%3Aconnect&resource=https%3A%2F%2Fmcp.figma.com%2Fmcp` (+ PKCE `code_challenge`)
- **Authentication completed:** **YES** — token berhasil mendarat di disk (verifikasi `hermes mcp test` + `_oauth_tokens_present` lolos):
  - `mcp-tokens/figma.json` (245 B): `access_token: figu_WtdeDB...` (Bearer, `expires_in: 7776000` ≈ 90 hari, `expires_at: 1798021313.86`), `refresh_token: figur_5bbn9...`
  - `mcp-tokens/figma.client.json` (562 B): `client_name: "Claude Code"`, `client_id: g7FRsEwT...`, `client_secret: aHsuKA...`, `redirect_uris: ["http://127.0.0.1:51796/callback"]`, `token_endpoint_auth_method: client_secret_post`, `scope: "mcp:connect openid email"`
  - `mcp-tokens/figma.meta.json` (805 B): `issuer: https://api.figma.com`, `authorization_endpoint: https://www.figma.com/oauth/mcp`, `token_endpoint: https://api.figma.com/v1/oauth/token`, `registration_endpoint: https://api.figma.com/v1/oauth/mcp/register`
  - ACL Windows (via `icacls`): `NT AUTHORITY\SYSTEM:(F) / BUILTIN\Administrators:(F) / DESKTOP-J3Q8GSV\hikari:(F)` — tidak world-readable.

---

## 4. Tool Discovery

- **Tools discovered:** **40** — via `hermes mcp test figma` / `tools/mcp_tool_discovery`:
  1. `get_screenshot` — Generate a screenshot for a given node or the currently selected node
  2. `get_design_context` — Get design context for a Figma node — primary tool for dev
  3. `get_motion_context` — Get keyframe animation data for a Figma node
  4. `get_metadata` — Get metadata (prefer `get_design_context`)
  5. `get_variable_defs` — Get variable definitions for a given node id
  6. `get_figjam` — Generate UI code for a given FigJam node
  7. `generate_figma_design` — Capture a live web page by URL into an *existing* Figma design
  8. `generate_diagram` — Create a flowchart, decision tree, gantt, sequence diagram
  9. `get_code_connect_map` — Get mapping of {[nodeId]: {codeConnectSrc}}
  10. `whoami` — Returns authenticated user's handle, email, plan, teams
  11. `weave_list_tools` — Lists Weave tools the authenticated user can run
  12. `weave_get_tool_inputs` — Gets input contract of a Weave tool
  13. `weave_run_tool` — Runs a Weave tool
  14. `weave_upload_asset` — Uploads local image/video to Weave
  15. `weave_get_tool_run_output` — Gets output/status of Weave tool runs
  16. `weave_cancel_tool_run` — Cancels in-progress Weave tool runs
  17. `weave_find_model` — Finds a Weave AI model by name
  18. `weave_run_model` — Runs a Weave AI model directly
  19. `weave_get_model_run_output` — Gets output/status of Weave model runs
  20. `add_code_connect_map` — Map a Figma node to a code component
  21. `get_code_connect_suggestions` — Get AI-suggested Code Connect strategy
  22. `send_code_connect_mappings` — Save multiple Code Connect mappings in bulk
  23. `export_video` — Export a Figma timeline node as MP4
  24. `get_context_for_code_connect` — Get structured component metadata
  25. `list_file_components_for_code_connect` — List every component/component set published to a file
  26. `use_figma` — Create, edit, generate, or sync any design in Figma
  27. `get_libraries` — Get design libraries associated with a Figma file
  28. `search_design_system` — Search for design system assets (components, variables)
  29. `create_new_file` — Create a new blank Figma file (write — requires skill load)
  30. `upload_assets` — Upload assets (images/SVGs) into a Figma file
  31. `download_assets` — Download assets from a Figma file for a single node
  32. `list_file_shaders` — Lists shader effects/fills used in a Figma file
  33. `list_shaders` — Lists shader effects/fills in the authenticated account
  34. `get_shader` — Reads a shader effect/fill from account library
  35. `list_generative_plugins` — Lists generative plugins in account
  36. `get_generative_plugin` — Reads a generative plugin by id
  37. `create_generative_plugin` — Create generative plugin (requires skill)
  38. `create_shader` — Create shader (requires skill)
  39. `update_generative_plugin` — Update generative plugin (requires skill)
  40. `update_shader` — Update shader (requires skill)
- **Resources/prompts discovered:** Server fokus ke tools; `hermes mcp test` tidak melaporkan resources/prompts terpisah — ini expected untuk Figma remote MCP, bukan error. Tidak ada transport/schema/auth error.
- **Functional MCP call successful:** **YES** — safe read-only invocation via `hermes chat --oneshot` (session `20260924_172903_13d99b`, workspace `UI-UX`, model `ag/gemini-3-flash` via `9router`):
  ```
  Query: Use the Figma MCP tool 'whoami' and tell me my Figma handle, email and plan type.
         Do not create or modify any Figma file.

  Result:
  • Handle: shapiere
  • Email: shapierejanuarr@gmail.com
  • Plan Type: Starter (Free tier)
  • Teams: Shapiere Januar Rafiansyah's team (Starter, Admin) / kel 13_ui/ux semest 2 (Starter, Member) / UI/UX 13 (Starter, Member)
  ```
  Tidak ada file write, tidak ada modifikasi canvas. Verifikasi non-destruktif selesai.

---

## 5. Safety Check

- **Existing MCP servers preserved:** **YES** — sebelum install: 0 server (`No MCP servers configured`). Sesudah: hanya `figma` yang ditambahkan via `hermes mcp install figma` (catalog flow mempertahankan server lain; `tools.include` tidak ditulis karena all 40 enabled — `no filter`). Tidak ada penghapusan/overwrite. Backup otomatis tersimpan di `config.backup.yaml`.
- **Credential exposure detected:** **NO** — `config.yaml` hanya berisi `url` + `auth: oauth` + `enabled: true`; tidak ada `token`/`secret` plaintext. Token terisolasi di `mcp-tokens/*.json` dengan ACL terbatas; `security.redact_secrets` ON (default). Tidak ada `headers.Authorization` plaintext.
- **Unrelated files modified:** **NO** — diff `config.backup.yaml → config.yaml` hanya additive `mcp_servers.figma` block + migrasi otomatis komentar (`_config_version 41→42`, `memory`/`platforms.telegram`). Tidak ada perubahan `model`/`providers` (`9router`/`ag/gemini-3-flash`), `display`, `skills`, `hooks`, atau platform files. Workspace `G:/Course LSM/UI-UX` tetap kosong sebelum report ini dibuat; tidak ada file HTML/React/prototype tugas yang dibuat sesuai instruksi FASE 7.
- **Destructive operation performed:** **NO** — hanya `whoami` (read) yang dieksekusi. Tidak ada `create_new_file`, `upload_assets`, `use_figma` write, atau delete. Pernyataan: write paths (mis. `create_new_file`) akan membuat file Figma baru — memerlukan konfirmasi Anda sebelum diuji; tidak dieksekusi pada tahap ini.

---

## 6. Problems

- **No blocking issues.** Catatan informatif (tidak terkait Figma, tidak menghambat):
  - `hermes doctor` warning: `model.default 'ag/gemini-3-flash' uses vendor/model slug but provider is '9router'` — pre-existing, tidak mempengaruhi MCP.
  - `hermes --version` info: `Update available: 7970 commits behind` — tidak di-update mid-verification kecuali Anda minta.
  - `hermes mcp test` exit code `0` kedua kali; tidak ada transport/auth/schema error.
  - Jika di masa depan Figma mengubah allowlist DCR `client_name`, re-auth via `hermes mcp login figma` mungkin diperlukan (saat ini sukses dengan `Claude Code`).

---

## 7. Final Status

**READY** — Figma MCP berhasil terhubung dan aman untuk tahap berikutnya.

Konfigurasi valid (transport Streamable HTTP benar, endpoint `https://mcp.figma.com/mcp` tepat, schema sesuai `mcp_servers` OMP v0.21.1), OAuth selesai (tokens cached + refreshable), 40 tools discovered, functional call `whoami` sukses (read-only), tidak ada perubahan destruktif atau credential exposure.

**Next step menunggu instruksi Anda.** Tidak melanjutkan ke pembuatan prototype peminjaman buku sampai Anda perintahkan. Jika ingin menguji write capability (`create_new_file`, `upload_assets`, `use_figma`), konfirmasi dulu — pengujian tersebut akan membuat resource baru di akun Figma Anda.

---

### Appendix — Evidence & Repro

```bash
# Versi & path
hermes --version
# Hermes Agent v0.21.1 (2026.9.7) · upstream 7de8728c · local 1603a073 (+1)
hermes config path
# C:\Users\hikari\AppData\Local\hermes\config.yaml
hermes config env-path
# C:\Users\hikari\AppData\Local\hermes\.env

# Pre-install check
hermes mcp list
# No MCP servers configured.
hermes mcp catalog | grep figma
# figma  available  Official Figma remote MCP — design context, Code Connect, and write-to-canvas via https://mcp.figma.com/mcp (OAuth).

# Install (catalog — handles DCR + OAuth)
hermes mcp install figma
# → browser OAuth → checklist 40 tools → ✓ Installed 'figma' (enabled).

# Post-install
hermes mcp list
# figma  https://mcp.figma.com/mcp  all  ✓ enabled
hermes mcp test figma
# ✓ Connected (1563ms)  ✓ Tools discovered: 40
hermes config check
# Config version: 42 ✓

# Safe functional test (read-only)
hermes chat -q "Use the Figma MCP tool 'whoami' and tell me my Figma handle, email and plan type. Do not create or modify any Figma file." --oneshot --quiet
# → shapiere / shapierejanuarr@gmail.com / Starter (Free)
```

**Files changed in this verification:**
- `C:\Users\hikari\AppData\Local\hermes\config.yaml` — added `mcp_servers.figma` (additive, backup at `config.backup.yaml`)
- `C:\Users\hikari\AppData\Local\hermes\mcp-tokens/figma.json`, `figma.client.json`, `figma.meta.json` — OAuth tokens (auto-created, ACL-restricted)
- `G:/Course LSM/UI-UX/FIGMA_MCP_AUDIT_REPORT.md` — this report (only file in workspace)
